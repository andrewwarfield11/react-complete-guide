import React from 'react';

// note: can be used as an empty wrapper to ensure that you're returning only one element enclosing all the others
const aux = props => props.children;

export default aux;